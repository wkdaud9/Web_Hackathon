# Learnings & Patterns

## Project Structure
- Docs are in `Docs/`
- Source in `src/`
- Data mocks should be loaded from provided JSON files (need to locate them)

## Best Practices
- Use `localStorage` wrapper for all data access to simulate DB.
- Use Tailwind for styling.
