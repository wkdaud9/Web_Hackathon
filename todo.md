# 작업 현황 및 To-Do List (2026-03-23)

## ✅ 오늘 완료된 작업
- **Git 업로드**: 현재까지의 모든 소스 코드를 `origin/main`에 커밋 및 푸시 완료.
- **회원가입/로그인 버그 수정**: 로그아웃 후 다시 로그인 버튼을 눌렀을 때 기존 회원가입 정보가 남아있던 문제 해결 (모달 오픈 시 상태 초기화 로직 추가).
- **드롭다운 UI 개선**: 팀 모집 라운지 등에서 해커톤 이름이 길어질 경우 두 줄로 깨지는 현상 수정 (말줄임표 처리 및 레이아웃 고정).
- **메시지(쪽지) 시스템 기초 설계**:
  - `Message` 인터페이스 정의 (`models.ts`)
  - 로컬 스토리지를 활용한 쪽지함 API 구현 (`api.ts`: `getMessages`, `sendMessage`, `getUnreadCount` 등)
- **알림(Toast) 시스템 도입**: 기존 브라우저 기본 `alert`을 대체할 예쁜 토스트 알림 컴포넌트 및 컨텍스트 구현 (`ToastContext.tsx`).
- **마이페이지 기초 구현**: `/mypage` 경로 추가 및 유저 프로필 섹션 레이아웃 작업.

## 🚀 내일 이어서 해야 할 작업
1. **메시지(쪽지) UI 실구현**:
   - 헤더(Layout)에 쪽지함 아이콘 추가 및 읽지 않은 쪽지 개수 배지 표시.
   - 클릭 시 쪽지 목록을 볼 수 있는 드롭다운 또는 모달 구현.
   - 팀 모집 라운지 등에서 쪽지 보내기 완료 시 `showToast`를 사용하여 예쁜 알림 띄우기.
2. **마이페이지(MyPage) 고도화**:
   - 유저가 참여 중인 해커톤 목록 표시.
   - 유저가 구성한 팀 목록 및 모집 상태 표시.
3. **토스트(Toast) 적용 확대**:
   - 현재 `alert()`으로 구현된 모든 안내 메시지(수정 완료, 삭제 완료, 로그인 실패 등)를 `useToast` 기반으로 변경.
4. **전체적인 UI/UX 디테일 점검**:
   - 반응형 레이아웃에서 요소들이 겹치거나 깨지는 부분이 없는지 최종 확인.

---
**메모**: 모든 데이터는 현재 로컬 스토리지에 저장되므로, 브라우저 캐시를 삭제하면 초기화될 수 있음을 유의해주세요.
